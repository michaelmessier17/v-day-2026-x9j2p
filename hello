<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title>A Special Question for You</title>
    <style>
        :root {
            --primary-pink: #ff4d6d;
            --soft-pink: #fff0f3;
            --dark-pink: #d63384;
        }

        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: var(--soft-pink);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow-x: hidden;
        }

        #main-card {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(214, 51, 132, 0.2);
            text-align: center;
            max-width: 400px;
            width: 90%;
            position: relative;
        }

        .photo-container {
            margin-bottom: 25px;
        }

        #valentinesPhoto {
            width: 100%;
            max-width: 280px;
            height: auto;
            border-radius: 15px;
            border: 4px solid var(--soft-pink);
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        h1 {
            color: var(--dark-pink);
            font-size: 1.8rem;
            margin-bottom: 30px;
        }

        .btn-wrapper {
            display: flex;
            justify-content: center;
            gap: 20px;
            height: 60px;
            position: relative;
        }

        button {
            padding: 12px 25px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            border-radius: 50px;
            border: none;
            transition: all 0.2s ease;
        }

        #yesBtn {
            background-color: var(--primary-pink);
            color: white;
            z-index: 10;
        }

        #yesBtn:hover {
            transform: scale(1.1);
            background-color: #ff758f;
        }

        #noBtn {
            background-color: #ced4da;
            color: #495057;
            position: absolute; /* Allows it to move freely within the card */
        }

        #giftArea {
            display: none;
            animation: fadeIn 1s ease-in;
        }

        .gift-box {
            margin-top: 20px;
            padding: 20px;
            border: 2px dashed var(--primary-pink);
            border-radius: 10px;
            background-color: #fff9fa;
        }

        .claim-code {
            font-family: monospace;
            font-size: 1.4rem;
            color: var(--dark-pink);
            background: #eee;
            padding: 5px 10px;
            display: block;
            margin: 10px 0;
        }

        a.redeem-link {
            text-decoration: none;
            color: white;
            background: #232f3e; /* Amazon dark blue */
            padding: 8px 15px;
            border-radius: 5px;
            font-size: 0.9rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

    <div id="main-card">
        <div id="askArea">
            <div class="photo-container">
                <img src="us.jpg" alt="Us" id="valentinesPhoto">
            </div>
            <h1>Will you be my Valentine? ❤️</h1>
            <div class="btn-wrapper">
                <button id="yesBtn" onclick="showGift()">YES</button>
                <button id="noBtn" onmouseover="moveButton()">NO</button>
            </div>
        </div>

        <div id="giftArea">
            <h1 style="font-size: 2.5rem;">🥰 Yay!</h1>
            <p>I knew you'd say yes! You're the best.</p>
            <div class="gift-box">
                <p>Treat yourself to something nice:</p>
                <span class="claim-code">I've got an Amazon Gift card 4 u so u can buy a nick nack or something</span>
                <a href="https://www.amazon.com/gc/redeem" target="_blank" class="redeem-link">Redeem on Amazon</a>
            </div>
        </div>
    </div>

    <script>
        function moveButton() {
            const btn = document.getElementById('noBtn');
            const card = document.getElementById('main-card');
            
            // Calculate random positions within the bounds of the card
            // to keep the button from disappearing off-screen
            const maxX = card.clientWidth - btn.clientWidth;
            const maxY = card.clientHeight - btn.clientHeight;

            const randomX = Math.floor(Math.random() * maxX);
            const randomY = Math.floor(Math.random() * maxY);

            btn.style.left = randomX + 'px';
            btn.style.top = randomY + 'px';
        }

        function showGift() {
            document.getElementById('askArea').style.display = 'none';
            document.getElementById('giftArea').style.display = 'block';
            
            // Simple confetti effect (optional)
            document.body.style.backgroundColor = '#ffccd5';
        }
    </script>
</body>
</html>
